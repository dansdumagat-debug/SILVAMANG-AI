import 'package:flutter/material.dart';

class AppShadows {
  const AppShadows._();

  static const softCard = [
    BoxShadow(color: Color(0x14003C3A), blurRadius: 24, offset: Offset(0, 10)),
  ];

  static const button = [
    BoxShadow(color: Color(0x22064D35), blurRadius: 16, offset: Offset(0, 8)),
  ];
}
