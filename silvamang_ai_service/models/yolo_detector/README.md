# YOLOv8 Detector Model

Place the trained mangrove plant-part detector here:

```text
best.pt
```

Expected classes:

- leaves
- bark
- roots
- canopy

The `/ai/detect` endpoint will stay unavailable until a real trained detector model file is present.
