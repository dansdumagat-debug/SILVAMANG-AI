# YOLOv8 Segmentation Model

Place the trained mangrove segmentation model here:

```text
best.pt
```

This must be a YOLO segmentation model. A detection-only YOLO file is not enough for `/ai/segment`.
